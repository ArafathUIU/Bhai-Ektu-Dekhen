# Bhai Ektu Dekhen 👀

A community-powered civic issue reporting and tracking platform. Citizens report local infrastructure problems with photos and geolocation; AI + geospatial intelligence + community validation help authorities prioritize and resolve them.

> Full product definition and architecture: [`Bhai_Ektu_Dekhen_Project_Architecture.md`](./Bhai_Ektu_Dekhen_Project_Architecture.md)
> Implementation plan, progress log and checklists: [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md)

## Stack

| Layer | Tech |
|-------|------|
| Frontend | Next.js 16, TypeScript, Tailwind CSS, Leaflet |
| Backend | Laravel 13 (modular monolith), Sanctum token auth |
| Database | PostgreSQL + PostGIS (Docker, port 5433) |
| AI | Python worker (FastAPI: CV classification, severity, embeddings, duplicate detection) |
| Infra | Redis (queue/cache/rate-limit, port 6380), Docker Compose, GitHub Actions |

## Getting started

### Full stack (Docker)

```bash
docker compose up -d --build
```

Starts postgres, redis, ai-worker, backend, queue-worker, scheduler and frontend.

### Local development

#### 1. Infrastructure (PostgreSQL + PostGIS + Redis)

```bash
docker compose up -d postgres redis
```

Database on `localhost:5433` (user `bek`, password `bek_password_2026`, db `bek`); Redis on `localhost:6380`.

#### 2. Backend

```bash
cd backend
composer install
cp .env.example .env   # match docker-compose credentials
php artisan migrate --seed
php artisan serve      # http://localhost:8000
```

#### 3. AI worker

```bash
cd ai-worker
python -m venv .venv && .venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --port 9000
```

#### 4. Queue worker (needed for AI analysis jobs)

```bash
cd backend
php artisan queue:work --timeout=120 --tries=1
```

#### 5. Frontend

```bash
cd frontend
npm install
npm run dev           # http://localhost:3000
```

The Next.js app proxies `/api/*` and `/storage/*` to Laravel (see `frontend/next.config.ts`).

### Seed accounts

- `admin@bek.local` / `password` — ADMIN
- `citizen@bek.local` / `secret123` — USER

### API documentation

Interactive OpenAPI docs (auto-generated by Scramble) at `http://localhost:8000/docs/api`, or import [`docs/postman_collection.json`](./docs/postman_collection.json) into Postman.

## API (v1)

- Auth: `POST /api/v1/auth/register|login` · `POST /logout` · `GET /profile`
- Reports: `GET|POST /api/v1/reports` · `GET /api/v1/reports/{publicId}` · `POST /{publicId}/verify|reject`
- Issues: `GET /api/v1/issues` · `GET /{publicId}` · `POST /{publicId}/support` · `PATCH /{publicId}/status|severity`
- Map: `GET /api/v1/map/nearby` · `GET /api/v1/map/heatmap`
- Intelligence: `GET /api/v1/intelligence/hotspots|analytics` · `GET /priorities` (admin)
- Admin: `GET /api/v1/admin/dashboard` · `GET|POST /api/v1/admin/teams` · `POST /issues/{publicId}/assign` · `GET /admin/moderation`
- Notifications: `GET /api/v1/notifications` · `GET /unread-count` · `POST /read-all`

## Tests

```bash
cd backend && php artisan test          # 47 feature + unit tests (pgsql test DB)
cd ai-worker && pytest                 # AI worker unit tests
```

## Progress

Phases 1–7 are implemented: core reporting, maps, admin/moderation, AI duplicate detection, intelligence/hotspots/priorities, production hardening (caching, rate limiting, notifications, Docker, CI/CD), API docs and demo seed data. See the [implementation plan](./docs/IMPLEMENTATION_PLAN.md) for the full checklist and decisions log.